import type { Metadata } from "next";
import { Placeholder } from "../_components/Placeholder";

export const metadata: Metadata = { title: "Sensors" };

export default function SensorsPage() {
  return <Placeholder title="Sensors" />;
}
